🧮 NumPy Practice Questions (with Answers)
1. Create a NumPy array of numbers from 1 to 20.
2. Create a 4×4 matrix filled with random integers between 10 and 50.
3. Given an array, replace all values greater than 10 with 10.
4. Find the mean, median, and standard deviation of a NumPy array.
5. Normalize a NumPy array (min-max scaling).
6. Create a DataFrame with columns: name, age, score.
import pandas as pd
df = pd.DataFrame({
    "name": ["Alice", "Bob", "Cathy"],
    "age": [23, 35, 29],
    "score": [85, 90, 95]
})
7. Read a CSV file named sales.csv and show the first 5 rows.
8. Select rows where age > 30.
9. Add a new column called “status” where score ≥ 90 → “Pass”, else “Fail”.
10. Group the DataFrame by “status” and calculate the average age.
11. Generate a random array (3×4) with values 5–20.
12. Given an array, find all even numbers.
13. Reshape a 1D array of length 16 into a 4×4 matrix.
14. a = np.array([1,2,3])
b = np.array([4,5,6])
vertical = np.vstack((a, b))
horizontal = np.hstack((a, b))
15. Normalize an array (Min–Max and Z-score).
16. Find Euclidean distance between two vectors. ( np.linalg.norm(a - b) ) 
17. Remove duplicate rows from a 2D array.
18. Create 1000 random samples and compute stats.
samples = np.random.randn(1000)
mean = samples.mean()
var = samples.var()
p25, p50, p75 = np.percentile(samples, [25, 50, 75])
19. import pandas as pd
df = pd.DataFrame({
    'name': ['Alice', 'Bob', 'James'],
    'age': [25, 30, 22],
    'city': ['NY', 'LA', 'Chicago']
})
20. Select only the age column.
21. Select multiple columns
21. Filter rows where score > 80.
22. Replace missing values with mean.
23. Sort by age descending.
23. Select rows using index
24. Select rows based on condition
24. Drop a column.
25. Group by department: average salary, max age.
26. Merge two DataFrames on a key.
emp = pd.DataFrame({'ID':[1,2,3], 'Name':['A','B','C']}) <br>
sal = pd.DataFrame({'ID':[1,2,3], 'Salary':[1000,2000,3000]}) <br>
pd.merge(emp, sal, on='ID', how='inner')


27. Read large CSV, check memory, optimize types.
28. Detect and remove duplicate rows.
29. Apply a custom function to each row.
def grade(score):
    if score >= 90: return "A"
    elif score >= 80: return "B"
    else: return "C"

df['grade'] = df['score'].apply(grade)

30. Time-series: convert to datetime, set index, resample monthly.
monthly = df.resample('M').sum()

31. Replace all odd numbers with -1
arr = np.arange(10)
arr[arr % 2 == 1] = -1
print(arr)

32. Filter rows where Age > 25 and Salary < 60000
33. Fill missing values with mean
34. Drop rows with missing values
35. Concatenate two DataFrames
df1 = pd.DataFrame({'ID': [1,2], 'Name': ['A', 'B']}) <br>
df2 = pd.DataFrame({'ID': [3,4], 'Name': ['C', 'D']}) <br>
pd.concat([df1, df2])
36. Join using index
df1.join(df2, lsuffix='_left', rsuffix='_right')

37. Create new column based on condition
df['Status'] = np.where(df['Salary'] > 40000, 'High', 'Low')

38. Reset and set index
df.reset_index(inplace=True)
df.set_index('Name', inplace=True)

39. Read CSV and find top 5 highest-paid employees
40. Calculate average salary per department
41. Find total sales for each region
42. Read Excel with multiple sheets

43. Handle date/time column
df['Date'] = pd.to_datetime(df['Date']) <br>
df['Year'] = df['Date'].dt.year <br>

44. Filter last 30 days data
import datetime
cutoff = datetime.datetime.now() - datetime.timedelta(days=30)
recent = df[df['Date'] > cutoff]

45. Merge customer and transaction datasets
merged = pd.merge(customers, transactions, on='CustomerID', how='inner')

46. 

